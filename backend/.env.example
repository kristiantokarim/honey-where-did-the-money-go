# Database
DATABASE_URL=postgresql://exp_track:exp_track_password@localhost:5432/exp_track

# MinIO
MINIO_ENDPOINT=localhost
MINIO_PORT=9000
MINIO_ACCESS_KEY=minio_admin
MINIO_SECRET_KEY=minio_password
MINIO_BUCKET=receipts
MINIO_USE_SSL=false

# Google AI
GOOGLE_API_KEY=your_gemini_key_here
AI_PROVIDER=claude-code # or gemini

# CORS (comma-separated origins)
CORS_ORIGINS=http://localhost:5173

# JWT (required in production, defaults provided for dev)
JWT_ACCESS_SECRET=dev-access-secret-change-me
JWT_REFRESH_SECRET=dev-refresh-secret-change-me

# Resend (optional — emails logged to console if not set)
RESEND_API_KEY=
RESEND_FROM_EMAIL=noreply@example.com

# Frontend URL (for email links)
FRONTEND_URL=http://localhost:5173
